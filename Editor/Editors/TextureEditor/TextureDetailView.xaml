<UserControl x:Class="Editor.Editors.TextureDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Editor.Editors"
             xmlns:util="clr-namespace:Editor.Utilities.Converters"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=local:TextureEditor}"
             d:DesignHeight="1080" d:DesignWidth="400">
    <UserControl.Resources>
        <util:EnumDescriptionConverter x:Key="enumDescriptionConverter"/>
        <util:TextureSizeToStringConverter x:Key="textureSizeToStringConverter"/>
        <util:BoolToYesNoConverter x:Key="boolToYesNoConverter"/>
    </UserControl.Resources>
    <Grid>
        <StackPanel Orientation="Horizontal" Margin="5">
            <StackPanel.Resources>
                <Style TargetType="{x:Type TextBlock}">
                    <Setter Property="Margin" Value="5"/>
                </Style>
            </StackPanel.Resources>
            <StackPanel>
                <TextBlock Text="Name"/>
                <TextBlock Text="Dimension"/>
                <TextBlock Text="Size"/>
                <TextBlock Text="Format"/>
                <TextBlock Text="Import Date"/>
                <TextBlock Text="Data Size"/>
                <TextBlock Text="Mip Levels"/>
                <TextBlock Text="In Normal Map"/>
                <TextBlock Text="Is HDR"/>
            </StackPanel>
            <StackPanel>
                <TextBlock d:Text="TextureName"
                           Text="{Binding Texture.FileName}"/>
                <TextBlock d:Text="2D Texture"
                           Text="{Binding Texture.ImportSettings.Dimension, Converter={StaticResource enumDescriptionConverter}}"/>
                <TextBlock d:Text="1024 x 1024 (512 x 512)">
                    <TextBlock.Text>
                        <MultiBinding Converter="{StaticResource textureSizeToStringConverter}">
                            <Binding Path="."/>
                            <Binding Path="SelectedSliceBitmap"/>
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>
                <TextBlock d:Text="BC7 (RGBA) High Quality"
                           Text="{Binding Texture.FormatName}"/>
                <TextBlock d:Text="17-Apr-2025 12:11"
                           Text="{Binding Texture.ImportDate, StringFormat=dd-MMM-yyy HH:mm}"/>
                <StackPanel Orientation="Horizontal"
                            d:Visibility="Visible">
                    <StackPanel.Style>
                        <Style TargetType="{x:Type StackPanel}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedSlice}"
                                             Value="{x:Null}">
                                    <Setter Property="Visibility"
                                            Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Style>
                    <TextBlock d:Text="32 MB"
                               Text="{Binding DataSize}"/>
                    <TextBlock Text="("/>
                    <TextBlock d:Text="4 MB"
                               Text="{Binding SelectedSlice.RawContent.LongLength}"/>
                    <TextBlock Text=")"/>
                </StackPanel>
                <TextBlock d:Text="12"
                           Text="{Binding Texture.MipLevels}"/>
                <TextBlock d:Text="No"
                           Text="{Binding Texture.IsNormalMap, Converter={StaticResource boolToYesNoConverter}}"/>
                <TextBlock d:Text="No"
                           Text="{Binding Texture.IsHDR, Converter={StaticResource boolToYesNoConverter}}"/>
            </StackPanel>
        </StackPanel>
     </Grid>
</UserControl>
