import 'package:flutter/material.dart';

class DummyEditor extends StatelessWidget {
  const DummyEditor({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      backgroundColor: Colors.red,
    );
  }
}
